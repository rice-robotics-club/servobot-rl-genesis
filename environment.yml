name: genesis
channels:
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
      - genesis-world
      - rsl-rl-lib==2.2.4
      - torch
      - torchvision
      - tensorboard
      - PyYAML
      - pygame
