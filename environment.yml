name: genesis
channels:
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
      - genesis-world
      - rsl-rl-lib == 3.1.2  
      - torch
      - torchvision
      - tensorflow
      - tensorboard
      - PyYAML
      - pygame