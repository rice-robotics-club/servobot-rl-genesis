name: genesis
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch
  - torchvision
  - pyyaml
  - tensorboard
  - jsonschema
  - jsonschema-gentypes
  - tensordict
  - numpy<2.0
  - pip:
      - genesis-world
      - rsl-rl-lib
      - inputs
